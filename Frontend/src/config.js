// Configuration for the Tug of War application
export const CONFIG = {
  // Admin access token for creating teams
  SOCKET_SERVER_URL: import.meta.env.VITE_SOCKET_SERVER_URL,
  ADMIN_ACCESS_TOKEN: import.meta.env.VITE_ACCESS_CODE,

  
  // Application version
  VERSION: "1.0.0"
};
